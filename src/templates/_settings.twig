{# @var plugin \fork\alter\Plugin #}
{# @var settings \fork\alter\models\Settings #}

{% import '_includes/forms.twig' as forms %}

{{ forms.autosuggestField({
  label: "Alt text generator service"|t('app'),
  id: 'altTextGenerator',
  name: 'altTextGenerator',
  suggestEnvVars: true,
  suggestions: altTextGeneratorSuggestions,
  value: settings.altTextGenerator,
  size: 60,
  errors: settings.getErrors('altTextGenerator'),
}) }}

{{ forms.autosuggestField({
  label: "API token"|t('app'),
  id: 'apiToken',
  name: 'apiToken',
  suggestEnvVars: true,
  value: settings.apiToken,
  size: 60,
  errors: settings.getErrors('apiToken')
}) }}
